import { NgWillDirective } from './ng-will.directive';

describe('NgWillDirective', () => {
  it('should create an instance', () => {
    const directive = new NgWillDirective();
    expect(directive).toBeTruthy();
  });
});
